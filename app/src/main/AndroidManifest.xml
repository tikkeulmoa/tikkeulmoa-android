<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="tikkeulmoa.cimile.org">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:name=".util.ApplicationController"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".ui.community.CalendarActivity">
        </activity>
        <activity android:name=".ui.community.WriteActivity">
        </activity>
        <activity android:name=".ui.community.NoticeAllActivity">
        </activity>
        <activity android:name=".ui.community.CommunityMakingActivity">
        </activity>
        <activity android:name=".ui.community.CommunityDetailActivity">
        </activity>
        <activity android:name=".ui.account.AccountDetailActivity">
        </activity>
        <activity android:name=".ui.account.AccountMakingActivity">
        </activity>
        <activity android:name=".ui.login.LoginActivity">
        </activity>
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>

</manifest>